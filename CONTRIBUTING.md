# Contributing

Open an issue before starting a large change. Keep claim-preservation behavior deterministic and cover domain changes with tests.

1. Fork and create a focused branch.
2. Run `npm install`.
3. Make the change and add tests.
4. Run `npm run check` and `npm audit`.
5. Submit a concise pull request describing user impact and privacy implications.
